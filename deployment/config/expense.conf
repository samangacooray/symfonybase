# Reference config — actual deployed file is deployment/vagrant/deploy-files/expenses-apache.conf
#
# Symfony API  → http://dev.expenses.com:8080  (DocumentRoot: expense/public)
# Angular SPA  → http://dev.expenses.com:80    (DocumentRoot: expense-spa/dist/expense-spa/browser)
# /api proxy   → port 80 proxies to port 8080
